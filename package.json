{
	"name": "atlas-bes",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "bun --watch src/server.ts",
		"start": "bun src/server.ts",
		"test": "bun test",
		"db:reset": "docker compose exec -T db psql -v ON_ERROR_STOP=1 -U postgres -d atlas < db/local/schema.sql",
		"db:seed": "docker compose exec -T db psql -v ON_ERROR_STOP=1 -U postgres -d atlas < db/local/seed.sql",
		"db:sync": "bun run db:reset && bun run db:seed && bun run db:codegen",
		"db:codegen": "kysely-codegen --dialect postgres --out-file src/db/types.ts",
		"auth:schema": "docker compose exec -T db psql -U postgres -c 'DROP DATABASE IF EXISTS better_auth_ddl' -c 'CREATE DATABASE better_auth_ddl' && bunx @better-auth/cli generate --config scripts/better-auth-cli-config.ts --output db/local/spec/better-auth-schema.sql --yes && docker compose exec -T db psql -U postgres -c 'DROP DATABASE IF EXISTS better_auth_ddl'",
		"lint": "biome check .",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@fastify/autoload": "^6.5.0",
		"@fastify/cookie": "^11.1.2",
		"@fastify/cors": "^11.3.0",
		"@fastify/env": "^7.0.0",
		"@fastify/helmet": "^13.1.0",
		"@fastify/rate-limit": "^11.1.0",
		"@fastify/sensible": "^6.0.4",
		"@fastify/swagger": "^9.8.1",
		"@fastify/swagger-ui": "^6.1.0",
		"@fastify/type-provider-typebox": "^6.1.0",
		"@fastify/under-pressure": "^9.1.0",
		"@sinclair/typebox": "^0.34.52",
		"better-auth": "^1.6.25",
		"fastify": "^5.10.0",
		"fastify-plugin": "^6.0.0",
		"kysely": "^0.29.4",
		"pg": "^8.22.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.5",
		"@testcontainers/postgresql": "^12.0.4",
		"@types/pg": "^8.20.0",
		"bun-types": "^1.3.14",
		"kysely-codegen": "^0.20.0",
		"testcontainers": "^12.0.4",
		"typescript": "^7.0.2"
	}
}
